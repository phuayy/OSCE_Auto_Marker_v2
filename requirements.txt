# Canonical Python runtime for the FastAPI/Hatchet backend and AI pipeline.
# Reviewed on 2026-07-08 against the tested local venv (Python 3.12.10).
#
# Supported target: Python 3.10-3.13.
# - Python 3.12 is the tested local development runtime.
# - Python 3.14 is intentionally excluded for now because WhisperX 3.8.6
#   declares Python <3.14 support.
# - NumPy uses environment markers so Python 3.10 receives the latest
#   Python-3.10-compatible line while Python 3.11+ gets the current fastest line.

# FastAPI application server and request parsing.
fastapi==0.136.3
uvicorn[standard]==0.49.0
python-multipart==0.0.32
pydantic==2.13.4

# Authentication and API tests.
bcrypt==5.0.0
pytest==9.0.3
httpx==0.28.1

# Durable queue support. Local development can use SQLite plus in-process
# workers; production uses Hatchet backed by PostgreSQL.
SQLAlchemy==2.0.50
# Schema migrations for both database layers (ORM tables and the raw-SQL jobs
# tables). Run with "cd fastapi_backend && alembic upgrade head"; the app also
# applies them at startup unless DB_AUTO_MIGRATE=false. See
# fastapi_backend/alembic/README.md.
alembic==1.19.1
aiosqlite==0.22.0
hatchet-sdk==1.33.7
psycopg[binary,pool]==3.3.4

# OpenAI-compatible client used by NVIDIA/OpenRouter scoring scripts.
openai==2.41.1

# Rubric parsing and audio/bell-analysis pipeline scripts.
pypdf==6.13.2
numpy==2.2.6; python_version == "3.10"
# 2.4.4 (not 2.4.6) is what the CUDA torch 2.8.0+cu128 stack was installed and
# tested against locally; keep the two moving in lockstep.
numpy==2.4.4; python_version >= "3.11" and python_version < "3.14"
librosa==0.11.0
soundfile==0.14.0
opensmile==2.6.0

# WhisperX CLI used by WHISPERX_BIN for transcription/diarization.
# If this conflicts with your CUDA/PyTorch stack, install WhisperX separately
# in the same environment using your GPU-specific PyTorch/WhisperX instructions.
whisperx==3.8.6

# WhisperX-compatible ML stack constraints. These prevent slow resolver
# backtracking and keep ASR/diarization installs reproducible.
# NOTE (GPU): PyPI serves CPU-only torch wheels. For the local RTX GPU install
# the CUDA 12.8 builds instead (same versions, satisfies these pins):
#   pip install torch==2.8.0 torchaudio==2.8.0 torchvision==0.23.0 \
#     --index-url https://download.pytorch.org/whl/cu128
torch==2.8.0
torchaudio==2.8.0
torchvision==0.23.0
transformers==4.57.6
tokenizers==0.22.2

# Person-presence auto-crop segmentation (scripts/detect_human_segments.py):
# RT-DETR runs through the transformers stack above; Pillow is required by
# transformers' image processors at runtime (frames are decoded via ffmpeg,
# so no OpenCV dependency).
pillow==12.2.0
